spring:

  datasource:
    url: 'jdbc:postgresql://localhost:5432/user-service-db'
    username: 'postgres'
    password: 'postgres'

  docker:
    compose:
      enabled: 'false'

  cache:
    type: 'redis'
    redis:
      host: 'localhost'
      port: '6379'

